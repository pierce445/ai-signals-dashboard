kimport { prisma } from "@/lib/prisma";

export default async function Home() {
  const capexRows = await prisma.signalObservation.findMany({
    where: {
      signalKey: "hyperscaler_capex",
    },
    orderBy: {
      observedAt: "desc",
    },
    take: 20,
  });

  const uniqueEntities = [...new Set(capexRows.map((row) => row.entity))];
  const latestObservedAt = capexRows[0]?.observedAt ?? null;

  const cards = [
    {
      title: "NVIDIA Data Center Revenue",
      description:
        "Watch the clearest real-time proxy for AI infrastructure demand.",
      status: "Coming soon",
    },
    {
      title: "Power Demand",
      description:
        "Monitor whether grid, cooling, and electrical equipment are becoming bottlenecks.",
      status: "Coming soon",
    },
    {
      title: "AI Networking",
      description:
        "Track 400G to 800G upgrades and AI cluster networking demand.",
      status: "Coming soon",
    },
    {
      title: "Enterprise AI Adoption",
      description:
        "See whether AI is becoming real revenue and workflow adoption, not just experimentation.",
      status: "Coming soon",
    },
  ];

  return (
    <main className="min-h-screen bg-neutral-950 text-white">
      <section className="mx-auto max-w-7xl px-6 py-16">
        <div className="mb-12">
          <p className="mb-3 text-sm uppercase tracking-[0.2em] text-neutral-400">
            AI Infrastructure Investing
          </p>
          <h1 className="text-4xl font-semibold tracking-tight sm:text-6xl">
            AI Signals Dashboard
          </h1>
          <p className="mt-4 max-w-3xl text-lg text-neutral-300">
            A live dashboard for tracking the five signals that matter most in the AI
            infrastructure cycle: capex, NVIDIA data center growth, power constraints,
            networking upgrades, and enterprise AI adoption.
          </p>
        </div>

        <div className="mb-10 grid gap-4 md:grid-cols-3">
          <div className="rounded-2xl border border-neutral-800 bg-neutral-900 p-5">
            <p className="text-sm text-neutral-400">Project Status</p>
            <p className="mt-2 text-2xl font-semibold">Phase 2</p>
            <p className="mt-2 text-sm text-neutral-300">
              Data layer connected and first live signal wired in.
            </p>
          </div>

          <div className="rounded-2xl border border-neutral-800 bg-neutral-900 p-5">
            <p className="text-sm text-neutral-400">Next Build Step</p>
            <p className="mt-2 text-2xl font-semibold">Real SEC Data</p>
            <p className="mt-2 text-sm text-neutral-300">
              Replace seeded capex rows with live company filings data.
            </p>
          </div>

          <div className="rounded-2xl border border-neutral-800 bg-neutral-900 p-5">
            <p className="text-sm text-neutral-400">Update Frequency</p>
            <p className="mt-2 text-2xl font-semibold">Daily</p>
            <p className="mt-2 text-sm text-neutral-300">
              Signals will eventually refresh automatically every day.
            </p>
          </div>
        </div>

        <div className="grid gap-6 md:grid-cols-2 xl:grid-cols-3">
          <div className="rounded-2xl border border-emerald-500/30 bg-neutral-900 p-6 shadow-lg shadow-black/20">
            <div className="flex items-start justify-between gap-4">
              <h2 className="text-xl font-semibold">Hyperscaler Capex</h2>
              <span className="rounded-full border border-emerald-500/30 bg-emerald-500/10 px-3 py-1 text-xs font-medium text-emerald-300">
                Live sample data
              </span>
            </div>

            <p className="mt-4 text-sm leading-6 text-neutral-300">
              Track Microsoft, Amazon, Alphabet, and Meta capital spending trends.
            </p>

            <div className="mt-6 space-y-3 text-sm text-neutral-300">
              <div className="flex items-center justify-between">
                <span className="text-neutral-400">Rows stored</span>
                <span className="font-medium text-white">{capexRows.length}</span>
              </div>

              <div className="flex items-center justify-between">
                <span className="text-neutral-400">Companies tracked</span>
                <span className="font-medium text-white">
                  {uniqueEntities.length}
 
               </span>
              </div>

              <div>
                <p className="text-neutral-400">Entities</p>
                <p className="mt-1 font-medium text-white">
                  {uniqueEntities.length > 0 ? uniqueEntities.join(", ") : "None yet"}
                </p>
              </div>

              <div>
                <p className="text-neutral-400">Latest observed</p>
                <p className="mt-1 font-medium text-white">
                  {latestObservedAt
                    ? new Date(latestObservedAt).toLocaleString()
                    : "No data yet"}
                </p>
              </div>
            </div>
          </div>

          {cards.map((card) => (
            <div
              key={card.title}
              className="rounded-2xl border border-neutral-800 bg-neutral-900 p-6 shadow-lg shadow-black/20"
            >
              <div className="flex items-start justify-between gap-4">
                <h2 className="text-xl font-semibold">{card.title}</h2>
                <span className="rounded-full border border-amber-500/30 bg-amber-500/10 px-3 py-1 text-xs font-medium text-amber-300">
                  {card.status}
                </span>
              </div>
              <p className="mt-4 text-sm leading-6 text-neutral-300">
                {card.description}
              </p>
            </div>
          ))}
        </div>
      </section>
    </main>
  );
}
